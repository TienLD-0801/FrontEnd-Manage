// format date const
export const DATE_FORMAT = 'dd/MM/yyyy';

// format date input
export const FORMAT_INPUT = 'DD/MM/YYYY';

// func get New Date
export const getNewDate = (date: string | number | Date): Date => {
  return new Date(date);
};
